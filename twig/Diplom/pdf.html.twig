<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8" />

<style>
@page {
    size: A4 portrait;
    margin: 0;
}

html, body {
    margin: 0;
    padding: 0;
}

body {
    font-family: 'TWKBurns Ultra', Arial, sans-serif;
    font-weight: normal;
}

* {
    font-family: 'TWKBurns Ultra', Arial, sans-serif;
    font-weight: normal;
    box-sizing: border-box;
}

@font-face {
    font-family: 'TWKBurns Ultra';
    src: url('{{ fontPath }}') format('truetype');
    font-weight: normal;
    font-style: normal;
}

/* === A4 PAGE === */
.page {
    width: 210mm;
    height: 297mm;
    background: {{ theme.background }};
    color: {{ theme.textColor }};
    position: relative;
    overflow: hidden;
}

/* remove default paragraph margins (very important in Dompdf) */
p {
    margin: 0;
}

/* === CONTENT === */
.content {
    padding-top: 20mm;
    padding-left: 15mm;
    font-size: 36px;
    line-height: 1.1;
}

.line {
    font-size: 24px;
}

.name-line,
.place-line,
.season-line {
    font-size: 48px;
    margin-top: -4mm;
}

/* === LOGO === */
.logo-wrapper {
    text-align: center;
    margin-top: -20mm;
}

.logo {
    width: 250mm;   /* nesten full bredde */
    height: auto;
    display: block;
    margin: 0 auto;
}

/* === FOOTER === */
.footer {
    position: absolute;
    bottom: 3mm;      /* lavere på siden */
    left: 0;
    right: 0;
    text-align: center;
    font-size: 50mm;  /* mindre enn før */
    line-height: 0.9;
}
</style>
</head>

<body>

{% for person in personer %}

    {% set nameLen = person.navn|length %}
    {% set nameSize = 48 %}
    {% if nameLen > 42 %}
        {% set nameSize = 32 %}
    {% elseif nameLen > 34 %}
        {% set nameSize = 36 %}
    {% elseif nameLen > 26 %}
        {% set nameSize = 40 %}
    {% endif %}

    {% set placeLen = placeLabel|length %}
    {% set placeSize = 48 %}
    {% if placeLen > 42 %}
        {% set placeSize = 32 %}
    {% elseif placeLen > 34 %}
        {% set placeSize = 36 %}
    {% elseif placeLen > 26 %}
        {% set placeSize = 40 %}
    {% endif %}

    <div class="page">

        <div class="content">
            <p class="line">Navn</p>
            <p class="name-line" style="font-size: {{ nameSize }}px;">
                {{ person.navn }}
            </p>

            <p class="line" style="margin-top: 8mm;">Sted</p>
            <p class="place-line" style="font-size: {{ placeSize }}px;">
                {{ placeLabel }}
            </p>

            <p class="line" style="margin-top: 8mm;">År</p>
            <p class="season-line" style="font-size: 52px;">
                {{ seasonLabel }}
            </p>
        </div>

        <div class="logo-wrapper">
            <img class="logo"
                 src="{{ logoDataUri ? logoDataUri : logoPathRel }}"
                 alt="UKM logo" />
        </div>

        <div class="footer">
            Diplom
        </div>

    </div>

{% endfor %}

</body>
</html>
